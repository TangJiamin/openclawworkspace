# 2026-03-02 记忆

## 🔴 关键发现和修正

### Agent 编排问题发现

用户提出了两个关键问题，发现当前文档存在严重缺陷：

#### 问题 1: 视频生成流程缺失图片步骤

**用户原则**: 
- 必须先生成图片，再根据图片生成视频
- 禁止并行生成图片和视频
- 禁止直接从文本生成视频

**当前问题**:
- TOOLS.md 中写的是 "visual-agent 或 video-agent"，暗示二选一
- 没有强制要求视频生成必须先完成图片生成
- 可能导致直接从文本生成视频的违规流程

**修正方案**:
1. ✅ TOOLS.md: 明确标注 "video-agent ⚠️ 必须先完成 visual-agent"
2. ✅ 工作流图: 修改为 `visual-agent → video-agent` 的串行流程
3. ✅ AGENT-MATRIX-REPLAN.md: 添加 "⚠️ 关键原则" 章节
4. ✅ 明确禁止并行分支中包含视频生成

#### 问题 2: 质量审核职责不明确

**用户要求**:
- 质量审核需要审核文案、图片和视频
- 需要针对不同内容类型的检查项

**当前问题**:
- 职责描述只说"多维度质量检查"，不够具体
- 没有明确说明需要审核图片和视频
- 输出格式没有针对不同内容类型的评分

**修正方案**:
1. ✅ 职责描述: 改为 "多维度质量检查（文案、图片、视频）"
2. ✅ 检查维度: 新增文案质量、图片质量、视频质量、整体一致性
3. ✅ 工作流: 展示三重检查流程（文案→图片→视频→一致性）
4. ✅ 输出格式: 添加分项评分（copywriting/image/video/overall_consistency）

---

## 📋 修改的文件

1. **TOOLS.md**
   - 修改子 Agents 列表，添加 ⚠️ 标注
   - 修改工作流图，明确 visual-agent → video-agent 串行流程

2. **docs/AGENT-MATRIX-REPLAN.md**
   - Agent 5 (video-agent): 添加 "⚠️ 关键原则" 章节
   - Agent 5 (video-agent): 修改工作流，强制包含图片检查
   - Agent 6 (quality-agent): 扩展职责描述
   - Agent 6 (quality-agent): 扩展检查维度（文案/图片/视频/一致性）
   - Agent 6 (quality-agent): 修改工作流图，展示三重检查
   - Agent 6 (quality-agent): 扩展输出格式，添加分项评分
   - 并行分支章节: 添加视频生成的限制说明

---

## ✅ 验证清单

在创建真正的 orchestrate Skill 之前，必须确保：

1. ✅ 视频生成流程强制包含图片生成步骤
2. ✅ 质量审核明确包含文案、图片、视频三重检查
3. ✅ 所有 Agent 职责清晰，边界明确
4. ✅ 工作流图准确反映依赖关系
5. ✅ 输出格式完整，包含所有必要信息

---

## 🎯 下一步行动

需要创建实际的 orchestrate Skill 实现：

1. 编排器逻辑（确保依赖关系）
2. 子 Agent 创建（requirement-analyzer, content-planner, quality-reviewer）
3. 结果传递机制
4. 错误处理和重试
5. 最终结果整合

**重要**: 实现时必须严格遵循修正后的文档规范！

---

## 🚀 最终方案：Main Agent 自动编排（2026-03-02 11:39）

### 用户期望

用户明确表示：
> "我期望主agent，也是你，可以根据我的输入自动编排工作流，并按需串行或并行调用子智能体"

### 核心需求

1. ✅ **自动编排** - 根据输入自动判断需要哪些 Agents
2. ✅ **动态调度** - 按需串行或并行
3. ✅ **智能决策** - Main Agent 自己决定调用策略
4. ❌ **不使用预定义工作流** - 不需要 OpenProse/Lobster

### 解决方案

**在 Main Agent 的 AGENTS.md 中添加编排逻辑**:

1. **编排决策树** - 根据用户输入自动判断需要哪些 Agents
2. **并行机会识别** - 能并行的并行（requirement + research）
3. **强制依赖关系** - video-agent 必须等待 visual-agent
4. **使用 sessions_spawn** - 直接调用子 Agents

### 实施内容

**更新的文件**:
1. `AGENTS.md` - 添加"多 Agent 编排能力"章节
2. `docs/ORCHESTRATION-EXAMPLES.md` - 创建编排示例文档

**编排规则**:

```
用户输入
  ↓
分析需求类型（关键词识别）
  ↓
判断需要的 Agents
  ↓
识别依赖关系
  ├─ 可并行: requirement || research
  └─ 必须串行: visual → video
  ↓
调用 sessions_spawn
  ↓
整合结果
```

### 关键约束

1. **视频生成必须先有图片**
   - 禁止并行生成图片和视频
   - 禁止直接从文本生成视频

2. **质量审核必须包含所有内容**
   - 文案、图片、视频三重检查
   - 整体一致性评估

3. **超时控制**
   - 每个 Agent 有独立的超时限制

### 下一步

**Main Agent 现在可以**:
- ✅ 自动分析用户需求
- ✅ 判断需要哪些子 Agents
- ✅ 决定串行或并行执行
- ✅ 调用子 Agents
- ✅ 整合结果返回给用户

**测试方式**:
```
用户: 生成小红书内容，推荐5个AI工具
Main Agent: 自动编排 6 个子 Agents，返回完整结果
```
